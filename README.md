# Lab 4 — DevOps automation Belyakova

- `service.sh` — HTTP-сервис на python3 http.server
- `lab4-service.service` — systemd unit
- `lab4-healthcheck.sh` — HTTP healthcheck
- `ansible/` — inventory.example.ini и site.yml для развёртывания
